import { Outlet } from "react-router-dom";

const ClientProjectBuilderContainer = () => {
  return (
    <div className="">
      <Outlet />
    </div>
  );
};

export default ClientProjectBuilderContainer;
