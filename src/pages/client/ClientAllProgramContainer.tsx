import { Outlet } from "react-router-dom";


const ClientAllProgramContainer = () => {

    return (
        <div className="">
            <Outlet/>
        </div>
    );
};

export default ClientAllProgramContainer;
